# Server layer (Parts 12-18). Installed on top of requirements.txt.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1 # PostgreSQL 16 driver
pillow-heif>=0.16    # HEIC uploads from mobile capture
weasyprint>=62       # PDF reports from the shared verdict snapshot
python-docx>=1.1     # editable DOCX reports from the same snapshot
httpx>=0.27          # ASGI contract tests and service integrations
