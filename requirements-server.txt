# Server layer (Parts 12-18). Installed on top of requirements.txt.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
sqlalchemy>=2.0
alembic>=1.13
boto3>=1.34          # S3-compatible immutable evidence storage
pillow-heif>=0.16    # HEIC uploads from mobile capture
httpx>=0.27          # ASGI contract tests and service integrations
