Installable LMPC Field Android preview for local-network testing.

- Enroll through a one-time QR issued by LMPC Workbench.
- Capture front, back and optional side-panel evidence.
- Queue evidence offline and retry on the department LAN.
- See a plain-language explanation for every rule result.

This is a debug-signed preview APK that permits local HTTP. It is not a production-store build.
